(function(module) {

	"use strict";

	var DirectoryDatasource = function(directoryPath) {
		this._directory = directoryPath;
	};

	DirectoryDatasource.prototype.getData = function(password) {

	};

	DirectoryDatasource.prototype.setData = function(password, details, entries) {

	};

	module.exports = DirectoryDatasource;

})(module);
